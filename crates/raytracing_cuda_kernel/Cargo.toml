[package]
name = "raytracing_cuda_kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
cuda_std = { git = "https://github.com/Rust-GPU/Rust-CUDA.git", package = "cuda_std" }
glam = "0.29.3"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
panic = "abort"